<UserControl x:Class="FlightJobsDesktop.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:FlightJobsDesktop.Views.Home"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundPrimary}">
    <Grid Background="{StaticResource BackgroundPrimary}">
        <TabControl x:Name="TabControlHome"                    
                    MinHeight="400" 
                    MinWidth="1000"
                    Margin="15,0,5,0"
                    Style="{StaticResource ModernTabControl}"
                    Background="{StaticResource BackgroundPrimary}"
                    BorderBrush="{StaticResource BorderPrimary}"
                    BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource ModernTabItem}"/>
            </TabControl.Resources>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE80F;" Margin="0,0,8,0" FontSize="16" Foreground="{StaticResource AccentPrimaryLight}"/>
                        <Label Content="Home" FontSize="16" Style="{StaticResource CustomTabItem}" />
                    </StackPanel>
                </TabItem.Header>
                <views:ConnectorView x:Name="ConnectorViewControl" />
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE709;" Margin="0,0,8,0" FontSize="16" Foreground="{StaticResource AccentPrimaryLight}"/>
                        <Label Content="Practice" FontSize="16" Style="{StaticResource CustomTabItem}" />
                    </StackPanel>
                </TabItem.Header>
                <views:PracticeView/>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE774;" Margin="0,0,8,0" FontSize="16" Foreground="{StaticResource AccentPrimaryLight}"/>
                        <Label Content="Web App" FontSize="16" Style="{StaticResource CustomTabItem}" />
                    </StackPanel>
                </TabItem.Header>
                <Grid Background="{StaticResource BackgroundPrimary}">
                    <views:WebAppView x:Name="WebAppViewControl" />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
